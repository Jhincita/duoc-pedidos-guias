name: CI/CD Pipeline

on:
  push:
    branches: [ main ]

env:
  DOCKERHUB_USERNAME: ${{ secrets.DOCKERHUB_USERNAME }}
  DOCKERHUB_TOKEN: ${{ secrets.DOCKERHUB_TOKEN }}

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Set up JDK 17
        uses: actions/setup-java@v4
        with:
          java-version: '17'
          distribution: 'temurin'

      - name: Build ms-pedidos JAR
        run: |
          cd sistema-gestion-pedidos
          mvn clean package -DskipTests

      - name: Build ms-guias JAR
        run: |
          cd ms-guias
          mvn clean package -DskipTests

      - name: Login to Docker Hub
        uses: docker/login-action@v3
        with:
          username: ${{ env.DOCKERHUB_USERNAME }}
          password: ${{ env.DOCKERHUB_TOKEN }}

      - name: Build and push ms-pedidos
        uses: docker/build-push-action@v5
        with:
          context: ./sistema-gestion-pedidos
          push: true
          tags: ${{ env.DOCKERHUB_USERNAME }}/ms-pedidos:latest

      - name: Build and push ms-guias
        uses: docker/build-push-action@v5
        with:
          context: ./ms-guias
          push: true
          tags: ${{ env.DOCKERHUB_USERNAME }}/ms-guias:latest

      - name: Deploy to GCP VM
        uses: appleboy/ssh-action@v1.0.3
        with:
          host: ${{ secrets.GCP_VM_IP }}
          username: ${{ secrets.GCP_VM_USER }}
          key: ${{ secrets.GCP_SSH_PRIVATE_KEY }}
          script: |
            cd ~/app
            # Create .env file with GCS secrets (database is already hardcoded in compose)
            echo "GCS_BUCKET_NAME=${{ secrets.GCS_BUCKET_NAME }}" > .env
            echo "GCS_ACCESS_KEY=${{ secrets.GCS_ACCESS_KEY }}" >> .env
            echo "GCS_SECRET_KEY=${{ secrets.GCS_SECRET_KEY }}" >> .env
            # Pull latest images
            docker compose pull
            # Restart containers
            docker compose up -d
